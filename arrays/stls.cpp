#include<bits/stdc++.h>
using namespace std;

int main(){
    priority_queue<int> maxi ; //create a max heap means it gives out the maximum element
    priority_queue<int, vector<int>, greater<int>> mini ; //create a min heap means it gives out the minimum element

    maxi.push(1);
    maxi.push(2);

    return 0;
}